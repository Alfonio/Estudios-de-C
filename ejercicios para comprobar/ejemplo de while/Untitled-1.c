#include <stdio.h>

int main() {
    int i = 1; // Inicializamos la variable de control

    while (i <= 5) { // La condición del while
        printf("%d\n", i); // Imprimimos el valor de i
        i++; // Incrementamos i para avanzar al siguiente número
    }

    return 0;
}