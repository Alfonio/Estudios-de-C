#include <stdio.h>
//ESTE CODIGO NO SE PUEDE EJECUTAR, SOLO ES PARA COMPROBACION
int main(){
 /*escribir la siguiente sentencia en su equivalente IF

    printf ("%s", (i%2 == 0) ? "es par" : "es impar");
*/
 int i;
  // Supongamos que ya esta  asignado el valor de la variable i

    if (i % 2 == 0) {
        printf("es par");
    } else {
        printf("es impar");
    }

    // Resto del código

    return 0;
}
