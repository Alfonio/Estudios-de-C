#include <stdio.h>
//ESTE CODIGO NO SE PUEDE EJECUTAR, SOLO ES PARA COMPROBACION
int main(){
 /*escribir la siguiente sentencia en su equivalente IF
    x = (y<z) ? y : z;
*/
    
  int x, y, z;

    // Supongamos que ya tienes valores asignados a las variables y y z

    if (y < z) {
        x = y;
    } else {
        x = z;
    }

    // Resto del código

    return 0;
}
